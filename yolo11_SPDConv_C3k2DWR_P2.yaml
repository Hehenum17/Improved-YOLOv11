# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11 object detection model with P2-P5 outputs
# 融合了可扩张残差注意力模块(C3k2_DWR)和P2检测头

# Parameters
nc: 4 # number of classes
scales: # model compound scaling constants
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] # summary: 319 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs
  s: [0.50, 0.50, 1024] # summary: 319 layers, 9458752 parameters, 9458736 gradients, 21.7 GFLOPs
  m: [0.50, 1.00, 512] # summary: 409 layers, 20114688 parameters, 20114672 gradients, 68.5 GFLOPs
  l: [1.00, 1.00, 512] # summary: 631 layers, 25372160 parameters, 25372144 gradients, 87.6 GFLOPs
  x: [1.00, 1.50, 512] # summary: 631 layers, 56966176 parameters, 56966160 gradients, 196.0 GFLOPs

# Backbone (保持不变)
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, SPDConv, [128]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, SPDConv, [256]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, SPDConv, [512]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, SPDConv, [1024]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head (融合改进)
head:
  # ------ 上采样路径 (P5 -> P4 -> P3 -> P2) ------
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 11
  - [[-1, 6], 1, Concat, [1]]  # 12: cat backbone P4 (第6层输出)
  - [-1, 2, C3k2_DWR, [512, False]]  # 13: 使用可扩张残差注意力模块

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 14
  - [[-1, 4], 1, Concat, [1]]  # 15: cat backbone P3 (第4层输出)
  - [-1, 2, C3k2_DWR, [256, False]]  # 16 (P3/8输出)

  # 新增P2检测层
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 17
  - [[-1, 2], 1, Concat, [1]]  # 18: cat backbone P2 (第2层输出)
  - [-1, 2, C3k2_DWR, [128, False]]  # 19 (P2/4输出)

  # ------ 下采样路径 (P2 -> P3 -> P4 -> P5) ------
  - [16, 1, Conv, [256, 3, 2]]  # 20: 从P3(16)下采样
  - [[-1, 13], 1, Concat, [1]]  # 21: cat with P4(13)
  - [-1, 2, C3k2_DWR, [512, False]]  # 22 (P4/16输出)

  - [-1, 1, Conv, [512, 3, 2]]  # 23
  - [[-1, 10], 1, Concat, [1]]  # 24: cat with P5(10)
  - [-1, 2, C3k2_DWR, [1024, True]]  # 25 (P5/32输出)

  # 检测头 (P2, P3, P4, P5)
  - [[19, 16, 22, 25], 1, Detect, [nc]]  # Detect(P2, P3, P4, P5)